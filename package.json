{
  "name": "vehicle-rental-api",
  "version": "1.0.0",
  "description": "OCTALOGIC TECH BACKEND TEST (VEHICLE RENTAL API)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "start-server": "node app.js",
    "seq:mig:gen": "sequelize migration:generate --name=$NAME",
    "seq:mig": "sequelize db:migrate",
    "seq:mig:undo": "sequelize db:migrate:undo",
    "seq:seed:gen": "sequelize seed:generate --name=$NAME",
    "seq:seed": "sequelize db:seed:all",
    "seq:seed:last": "sequelize db:seed",
    "seq:seed:undo": "sequelize db:seed:undo:all",
    "seq:seed:undo:last": "sequelize db:seed:undo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "mysql2": "^3.6.3",
    "nodemon": "^3.0.1",
    "sequelize": "^6.35.0",
    "sequelize-cli": "^6.6.2"
  }
}
